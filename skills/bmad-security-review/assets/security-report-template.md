# Security Review — {story-id-or-title} — {date}

**Agent:** Kassandra
**Diff base:** {git-ref-or-HEAD~1}
**Classification:** {CRITICAL | HIGH | CLEAN}
**Config:** `blocking_severity={CRITICAL|HIGH}`, `model={model-id}`

## Executive Summary

{2–3 sentences. What matters most in this diff, what was clean, what the reviewer should read first. Kassandra's voice — no padding.}

## Findings

### [CRITICAL] {short finding title}

- **CWE / OWASP:** CWE-XXX / AXX:2021
- **Datei:** path/to/file.go:line
- **Beschreibung:** {what is wrong — one paragraph}
- **Impact:** {what happens if exploited — reputational, data, operational consequences}
- **Empfehlung:** {specific, actionable. Not "fix this" — "replace `fmt.Sprintf(query, userId)` with `db.QueryContext(ctx, \"SELECT ... WHERE id = $1\", userId)`"}
- **Referenz:** {OWASP ASVS V2.1.1, NIST AC-3, etc.}

### [HIGH] {title}

{same structure}

### [MEDIUM] {title}

{same structure}

### [LOW] {title}

{same structure}

### [INFO] {title}

{same structure — often positive findings, dependency-scan output, or attack-surface observations}

## Nebu Invariants Check

| Invariant                                   | Status |
|---------------------------------------------|:------:|
| Compliance RSP coverage                     | ✅ / ⚠️ / ❌ |
| `reason` field on compliance access         | ✅ / ⚠️ / ❌ |
| Audit-log immutability                      | ✅ / ⚠️ / ❌ |
| `instance_admin` notification (if in-scope) | ✅ / ⚠️ / ❌ |
| OIDC token validation (`iss`/`aud`/`exp`)   | ✅ / ⚠️ / ❌ |
| Matrix Power Level checks                   | ✅ / ⚠️ / ❌ |
| No hardcoded secrets                        | ✅ / ⚠️ / ❌ |
| TLS 1.3 enforcement                         | ✅ / ⚠️ / ❌ |
| AES-256-GCM correctness                     | ✅ / ⚠️ / ❌ |
| Ed25519 verify-before-accept                | ✅ / ⚠️ / ❌ |
| No secrets in logs / error messages         | ✅ / ⚠️ / ❌ |

{For every ❌ there must be a matching HIGH or CRITICAL finding above. ⚠️ means partial or not verifiable from the diff — state what verification would require.}

## Dependency Scan

{Include only if the diff contains `go.sum` / `go.mod` / `mix.lock` / `mix.exs` / `rebar.lock` changes. Otherwise omit this section entirely.}

- **Go (`govulncheck`):** {clean / N findings listed / tool absent}
- **Elixir (`mix deps.audit`):** {clean / N findings listed / tool absent}
- **Erlang (`rebar3`):** {clean / findings / tool absent}

## Overall Risk

| Severity  | Count |
|-----------|:-----:|
| CRITICAL  | 0 |
| HIGH      | 1 |
| MEDIUM    | 2 |
| LOW       | 3 |
| INFO      | 1 |

## Pipeline Decision

{One of the following, explicitly:}

- **CLEAN** — no CRITICAL / HIGH findings. Pipeline may proceed.
- **HIGH findings present, `blocking_severity: CRITICAL` (default)** — Pipeline proceeds with warning. HIGH findings must be scheduled as follow-up stories or explicitly accepted before the next release.
- **HIGH findings present, `blocking_severity: HIGH`** — Pipeline stops. User decision required.
- **CRITICAL findings present** — Pipeline stops. User decision required: fix, accept with written justification, or convert to follow-up story.

---

*Generated by Kassandra — BMAD Security Review Agent. This report is an immutable audit artifact — do not edit retrospectively; create a new review if re-analysis is required.*