# Session 2026-05-17 — Story 14.4 GDPR Right to Erasure

## Review Summary
- **Story:** 14.4 — GDPR Right to Erasure (`DELETE /api/v1/admin/users/{userId}`)
- **Verdict:** APPROVED (0 CRITICAL, 0 HIGH, 0 MEDIUM, 2 LOW)
- **Report:** `_bmad-output/implementation-artifacts/security-review-14-4-gdpr-deletion-2026-05-17.md`

## Findings Applied
- LOW-1: Audit metadata now records `keys_deleted: confirmed|failed_best_effort` — remediation applied
- LOW-2: `M_USER_DEACTIVATED` enumeration accepted as Matrix-spec-required — added to BOND.md accepted risks

## Patterns Observed
- GDPR deletion handler correctly inherits path traversal protection from `parseMxcURI`/`isSafePathSegment` — existing compliance package pattern works well
- Audit outcome granularity: handlers that have best-effort sub-steps should record sub-step outcomes in metadata, not just top-level "success"
- Pipeline abort on gRPC error codes: code-review caught missing `return` after `writeComplianceError` in NotFound case — this is a recurring risk pattern for switch-based gRPC error handlers. Check: every `writeComplianceError` in a switch case should be followed by `return` unless explicitly intended to fall through.

## New Pattern for MEMORY.md
Missing `return` after `writeComplianceError` in gRPC error switch cases is a recurring correctness risk (found in Story 14.4 code-review cycle 1). All future handlers using switch-based gRPC error mapping should be checked for this.
